{% extends 'base.html.twig' %}

{% block title %}全データ{% endblock %}

{% block body %}
{% for post in posts %}
    <div>
    <a href="{{ path('app_micro_post_show_one', {id: post.id}) }}">{{ post.title }}</a>
    </div>
{% endfor %}

{% endblock %}
